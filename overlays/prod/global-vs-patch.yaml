# Deploy the global VirtualService in istio-system namespace
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: global-traffic-capture
  namespace: istio-system  # Override namespace for global routing